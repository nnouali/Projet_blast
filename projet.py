import numpy as np

# matrice de score avec les bons para
def score_matrix (rows, colums) :
    matrix = [] 
    for i in range (rows):
        row = []
        for j in range (colums):
            score = 0
            row.apped(score)
        matrix.append()
    return matrix
#ou 
matrix = np.array([
    [0, 0, 30],
    [50, 5, 25],
    [8, 12, 22]
])

#test pour git